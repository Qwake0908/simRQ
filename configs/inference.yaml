# @package _global_

defaults:
  - model: null # 我们需要在 self 之前加载 model，以覆盖推理中未使用的默认模型参数
  - _self_
  - paths: default
  - logger: csv
  - trainer: default
  - hydra: default
  - data_loading: null
  - extras: default
  - experiment: null

task_name: "inference"
id: ${now:%Y-%m-%d}/${now:%H-%M-%S}
tags: ["inference"]

experiment: null

model:
  loss_function: null
  optimizer: null
  scheduler: null
  evaluator: null

callbacks:
  bq_writer:
    table_id: ???



# 推理时必须传入检查点路径
ckpt_path: ???
